log syslog all;
protocol device {};
protocol direct {
    disabled;
    ipv4;
    ipv6;
}

protocol static {
    ipv4;
}

filter no_default_v4 {
    if net = 0.0.0.0/0 then reject;
    accept;
}

filter no_default_v6 {
    if net = ::/0 then reject;
    accept;
}
